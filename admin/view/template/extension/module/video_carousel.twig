{{ header }}{{ column_left }}
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="pull-right">
        <button type="submit" form="form-module" data-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fa fa-save"></i></button>
        <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-default"><i class="fa fa-reply"></i></a></div>
      <h1>{{ heading_title }}</h1>
      <ul class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
        <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
      </ul>
    </div>
  </div>
  <div class="container-fluid">
    {% if error_warning %}
    <div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> {{ error_warning }}
      <button type="button" class="close" data-dismiss="alert">&times;</button>
    </div>
    {% endif %}
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><i class="fa fa-pencil"></i> {{ text_edit }}</h3>
      </div>
      <div class="panel-body">
        <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-module" class="form-horizontal">
          <div class="form-group">
            <label class="col-sm-2 control-label" for="input-name">{{ entry_name }}</label>
            <div class="col-sm-10">
              <input type="text" name="name" value="{{ name }}" placeholder="{{ entry_name }}" id="input-name" class="form-control" />
              {% if error_name %}
              <div class="text-danger">{{ error_name }}</div>
              {% endif %}
            </div>
          </div>
          <div class="form-group">
            <label class="col-sm-2 control-label" for="input-format">{{ entry_format }}</label>
            <div class="col-sm-10">
              <select name="format" id="input-format" class="form-control">
                {% if format == 'carousel' %}
                <option value="carousel" selected="selected">{{ text_carousel }}</option>
                {% else %}
                <option value="carousel">{{ text_carousel }}</option>
                {% endif %}
                {% if format == 'grid' %}
                <option value="grid" selected="selected">{{ text_grid }}</option>
                {% else %}
                <option value="grid">{{ text_grid }}</option>
                {% endif %}
              </select>
            </div>
          </div>
          <div class="form-group">
            <label class="col-sm-2 control-label" for="input-status">{{ entry_status }}</label>
            <div class="col-sm-10">
              <select name="status" id="input-status" class="form-control">
                {% if status %}
                <option value="1" selected="selected">{{ text_enabled }}</option>
                <option value="0">{{ text_disabled }}</option>
                {% else %}
                <option value="1">{{ text_enabled }}</option>
                <option value="0" selected="selected">{{ text_disabled }}</option>
                {% endif %}
              </select>
            </div>
          </div>
          <fieldset>
            <legend>{{ text_videos }}</legend>
            <table id="video-item" class="table table-striped table-bordered table-hover">
              <thead>
                <tr>
                  <td class="text-left">{{ entry_image }}</td>
                  <td class="text-left">{{ entry_youtube_url }}</td>
                  <td class="text-left">{{ entry_title }}</td>
                  <td class="text-right">{{ entry_sort_order }}</td>
                  <td></td>
                </tr>
              </thead>
              <tbody>
              {% set video_item_row = 0 %}
              {% for video_item in video_items %}
              <tr id="video-item-row{{ video_item_row }}">
                <td class="text-left"><a href="" id="thumb-image{{ video_item_row }}" data-toggle="image" class="img-thumbnail"><img src="{{ video_item.thumb }}" alt="" title="" data-placeholder="{{ placeholder }}" /></a>
                  <input type="hidden" name="video_item[{{ video_item_row }}][image]" value="{{ video_item.image }}" id="input-image{{ video_item_row }}" /></td>
                <td class="text-left"><input type="text" name="video_item[{{ video_item_row }}][youtube_url]" value="{{ video_item.youtube_url }}" placeholder="{{ entry_youtube_url }}" class="form-control" /></td>
                <td class="text-left"><input type="text" name="video_item[{{ video_item_row }}][title]" value="{{ video_item.title }}" placeholder="{{ entry_title }}" class="form-control" /></td>
                <td class="text-right"><input type="text" name="video_item[{{ video_item_row }}][sort_order]" value="{{ video_item.sort_order }}" placeholder="{{ entry_sort_order }}" class="form-control" /></td>
                <td class="text-right"><button type="button" onclick="$('#video-item-row{{ video_item_row }}').remove();" data-toggle="tooltip" title="{{ button_remove }}" class="btn btn-danger"><i class="fa fa-minus-circle"></i></button></td>
              </tr>
              {% set video_item_row = video_item_row + 1 %}
              {% endfor %}
                </tbody>
              <tfoot>
                <tr>
                  <td colspan="4"></td>
                  <td class="text-right"><button type="button" onclick="addVideoItem();" data-toggle="tooltip" title="{{ button_video_add }}" class="btn btn-primary"><i class="fa fa-plus-circle"></i></button></td>
                </tr>
              </tfoot>
            </table>
          </fieldset>
        </form>
      </div>
    </div>
  </div>
</div>
<script type="text/javascript"><!--
var video_item_row = {{ video_item_row }};

function addVideoItem() {
	html  = '<tr id="video-item-row' + video_item_row + '">';
    html += '  <td class="text-left"><a href="" id="thumb-image' + video_item_row + '" data-toggle="image" class="img-thumbnail"><img src="{{ placeholder }}" alt="" title="" data-placeholder="{{ placeholder }}" /></a><input type="hidden" name="video_item[' + video_item_row + '][image]" value="" id="input-image' + video_item_row + '" /></td>';
	html += '  <td class="text-left"><input type="text" name="video_item[' + video_item_row + '][youtube_url]" value="" placeholder="{{ entry_youtube_url }}" class="form-control" /></td>';
	html += '  <td class="text-left"><input type="text" name="video_item[' + video_item_row + '][title]" value="" placeholder="{{ entry_title }}" class="form-control" /></td>';
	html += '  <td class="text-right"><input type="text" name="video_item[' + video_item_row + '][sort_order]" value="" placeholder="{{ entry_sort_order }}" class="form-control" /></td>';
	html += '  <td class="text-right"><button type="button" onclick="$(\'#video-item-row' + video_item_row + '\').remove();" data-toggle="tooltip" title="{{ button_remove }}" class="btn btn-danger"><i class="fa fa-minus-circle"></i></button></td>';
	html += '</tr>';

	$('#video-item tbody').append(html);

	video_item_row++;
}
//--></script>
{{ footer }}
